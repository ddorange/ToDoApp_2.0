var app = app || {};$(function(){        'use strict';    var notes,        noteApp;    notes = new app.collection.NoteCollection();    noteApp = new app.view.AppView( {collection: notes} );});
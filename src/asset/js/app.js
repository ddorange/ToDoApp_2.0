/** * * https://github.com/tastejs/todomvc/tree/gh-pages/architecture-examples/backbone * */var app = app || {};$(function(){        'use strict';    var notes,        list,        menu,        noteModels = [],        menuModel,        i,        len;    for ( i=0, len=10; i<len; i+=1 ) {        noteModels[i] = new app.model.NoteModel();    }    // Noteモジュール    // コレクションのインスタンスの作成    notes = new app.collection.NoteCollection(noteModels);    // Viewのインスタンス作成    list = new app.view.ListView( {collection: notes} );        // Menuモジュール    menuModel = new app.model.MenuModel();    menu = new app.view.MenuView( {model: menuModel} );        // 描画開始    list.render();});